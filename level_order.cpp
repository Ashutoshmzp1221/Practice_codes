#include<bits/stdc++.h>
using namespace std;

class tree
{
    private:
        int data;
        tree *left, *right;
    public:
        tree(int data) {this -> data = data; left = right = nullptr};
        
};